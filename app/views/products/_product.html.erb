<div id="<%= dom_id product %>">
  <p>
    <strong>Name:</strong>
    <%= product.name %>
  </p>

  <p>
    <strong>Price:</strong>
    <%= product.price %>
  </p>

  <p>
    <% product.categories.each do |p| %>
      <%= p.name + " " %>
    <% end %>
  </p>

</div>
